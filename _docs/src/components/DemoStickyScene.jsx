import styled from 'styled-components';
import { StickyScene } from '@react-scrolly/scene'

import { borderedStyle } from '../config/shared-styles';

export const BorderedStickyScene = styled(StickyScene)`${borderedStyle}`;
