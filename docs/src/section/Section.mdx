---
name: Section
route: /section/Section
menu: Sections
---

import { Playground, PropsTable } from 'docz'
import { Page, Section, StickySection, PageContext } from '@intrasections/core'

import { sectionStyle } from './styles'

# Section
<Playground>
  <div style={{ width: '100%', background: '#B0E0E6' }}>
    <Page>
      <StickySection
        name="sticky1"
        renderNonSticky={
          <>
            <Section trackingId="section1" trackOnce style={sectionStyle}>
              {({scrolledRatio}) => <div>Section # 1: {scrolledRatio}</div>}
            </Section>
            <Section trackingId="section2" style={sectionStyle}>
              {({scrolledRatio}) => <div>Section # 2: {scrolledRatio}</div>}
            </Section>
            <Section trackingId="section3" style={sectionStyle}>
              {({scrolledRatio}) => <div>Section # 3: {scrolledRatio}</div>}
            </Section>
          </>
        }
      >
        {() => (
          <div style={{
              width: '50%',
              height: '100%',
              margin: '0 auto',
              background: '#B0A0E6',
              display: 'flex',
              justifyContent: 'center',
              alignContent: 'center',
              flexDirection: 'column',
            }}
          >
            <PageContext.Consumer>
              {({ activeSectionId }) => (
                <span>{activeSectionId}</span>
              )}
            </PageContext.Consumer>
          </div>
        )}
      </StickySection>
    </Page>
  </div>
</Playground>


## Props

<PropsTable of={Section} />
