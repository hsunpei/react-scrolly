---
name: Section
route: /section/section
menu: Sections
---

import { Playground, Props } from 'docz'
import {
  PageProvider,
  Section,
  StickySectionWithActiveInfo,
} from '@react-scrolly/core'

import {
  StickyScene,
} from '@react-scrolly/scene'

import { sectionStyle } from './styles'

# Section
<Playground>
  <div style={{ width: '100%', background: 'linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%)' }}>
    <PageProvider>
      <>
        <StickySectionWithActiveInfo
          renderNonSticky={
            <>
              <Section trackingId="section1" style={sectionStyle}>
                {({isIntersecting, scrolledRatio}) => <div>Section # 1: {`${isIntersecting}`}, {scrolledRatio}</div>}
              </Section>
              <Section trackingId="section2" style={sectionStyle}>
                {({isIntersecting, scrolledRatio}) => <div>Section # 2: {`${isIntersecting}`}, {scrolledRatio}</div>}
              </Section>
              <Section trackingId="section3" style={sectionStyle}>
                {({isIntersecting, scrolledRatio}) => <div>Section # 3: {`${isIntersecting}`}, {scrolledRatio}</div>}
              </Section>
            </>
          }
        >
          {({activeSection}) => (
            <div style={{
                width: '50%',
                height: '100%',
                margin: '0 auto',
                background: 'linear-gradient(to right, #43e97b 0%, #38f9d7 100%)',
                display: 'flex',
                justifyContent: 'center',
                alignContent: 'center',
                flexDirection: 'column',
              }}
            >
              {activeSection &&
                <span>{activeSection.id}: {activeSection.ratio}</span>
              }
            </div>
          )}
        </StickySectionWithActiveInfo>

        <StickyScene
          renderOverlay={
            <>
              <Section style={sectionStyle}>
                {({isIntersecting, scrolledRatio}) => <div>Section Sticky 1: {`${isIntersecting}`}, {scrolledRatio}</div>}
              </Section>
              <Section style={sectionStyle}>
                {({isIntersecting, scrolledRatio}) => <div>Section Sticky 2: {`${isIntersecting}`}, {scrolledRatio}</div>}
              </Section>
            </>
          }
        >
          {({isIntersecting}) => (
            <div style={{
                width: '50%',
                height: '100%',
                margin: '0 auto',
                background: 'linear-gradient(to right, #43e97b 0%, #38f9d7 100%)',
                display: 'flex',
                justifyContent: 'center',
                alignContent: 'center',
                flexDirection: 'column',
              }}
            >
              <span>Sticky: {`${isIntersecting}`}</span>
            </div>
          )}
        </StickyScene>
      </>
    </PageProvider>
  </div>
</Playground>


## Props

<Props of={Section} />
